<h1>import</h1>

<%= form_for(@vacancy) do |f| %>
    <div class="field">
      Id импортируемой вакансии<br>
      <%= f.text_field :id %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

<%= link_to 'Back', vacancies_path %>
